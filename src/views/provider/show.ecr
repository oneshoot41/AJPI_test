<h1>Show <%= provider.name %></h1>

<p><%= provider.name %></p>
<p>
  <%= link_to("Back", "/providers", class: "btn btn-light btn-sm") -%>
  <%= link_to("Edit", "/providers/#{provider.id}/edit", class: "btn btn-success btn-sm") -%>
  <%= link_to("Delete", "/providers/#{provider.id}?_csrf=#{csrf_token}", "data-method": "delete", "data-confirm": "Are you sure?", class: "btn btn-danger btn-sm") -%>
</p>
