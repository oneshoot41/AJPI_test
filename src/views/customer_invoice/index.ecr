<div class="row">
  <div class="col-sm-11">
    <h2>Factures client</h2>
  </div>
  <div class="col-sm-1">
    <a class="btn btn-success btn-sm" href="/customer_invoices/new">New</a>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Actions</th>
      </tr>
    <tbody>
    <%- customer_invoices.each do |customer_invoice| %>
      <tr>
        <td><%= customer_invoice.name %></td>
        <td>
          <span>
            <%= link_to("Show", "/customer_invoices/#{customer_invoice.id}", class: "btn btn-info btn-sm") -%>
            <%= link_to("Edit", "/customer_invoices/#{customer_invoice.id}/edit", class: "btn btn-success btn-sm") -%>
            <%= link_to("Delete", "/customer_invoices/#{customer_invoice.id}?_csrf=#{csrf_token}", "data-method": "delete", "data-confirm": "Are you sure?", class: "btn btn-danger btn-sm") -%>
          </span>
        </td>
      </tr>
    <%- end %>
    </tbody>
  </table>
</div>
