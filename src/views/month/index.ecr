<div class="row">
  <div class="col-sm-11">
    <h2>Months</h2>
  </div>
  <div class="col-sm-1">
    <a class="btn btn-success btn-sm" href="/months/new">New</a>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Actions</th>
      </tr>
    <tbody>
    <%- months.each do |month| %>
      <tr>
        <td><%= month.name %></td>
        <td>
          <span>
            <%= link_to("Show", "/months/#{month.id}", class: "btn btn-info btn-sm") -%>
            <%= link_to("Edit", "/months/#{month.id}/edit", class: "btn btn-success btn-sm") -%>
            <%= link_to("Delete", "/months/#{month.id}?_csrf=#{csrf_token}", "data-method": "delete", "data-confirm": "Are you sure?", class: "btn btn-danger btn-sm") -%>
          </span>
        </td>
      </tr>
    <%- end %>
    </tbody>
  </table>
</div>
